Use cases that compose `crypto/` and `data/` and hand the UI a simple
result. no Flutter imports

If a screen needs to do more than render UI and call one method, the
"more" belongs here, not in the screen
